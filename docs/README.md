# Docs

Project documents are organized by purpose.

- `dev-log/`: dated development logs
- `planning/`: planning documents, proposals, and feature drafts

When adding a new document, place it in the directory that best matches its intent.
